import React from 'react';
import './addItemModal.css';

const AddItemModal = () => {
  return (
    <div>addItemModal</div>
  )
}

export default AddItemModal



// have- add item modal- and -modal with form- in components just to pass robot check but
//i don't like the names so im not going to update the current files, and delete modal is different modal